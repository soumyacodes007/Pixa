version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: algopay-postgres
    environment:
      POSTGRES_USER: algopay
      POSTGRES_PASSWORD: algopay
      POSTGRES_DB: algopay
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U algopay"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
